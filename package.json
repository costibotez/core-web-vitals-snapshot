{
  "name": "core-web-vitals-snapshot",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension: a plain-English Core Web Vitals verdict for the site you are on. By Nomad Developer.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build --config vite.content.config.ts && vite build",
    "icons": "node scripts/generate-icons.mjs",
    "preview:popup": "vite preview"
  },
  "dependencies": {
    "web-vitals": "^5.3.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@types/chrome": "^0.0.287",
    "sharp": "^0.34.0",
    "typescript": "^5.7.0",
    "vite": "^7.0.0"
  }
}
