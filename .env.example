# Restricted, referrer-locked key for the free Chrome UX Report API.
# Create one at https://console.cloud.google.com/ (enable "Chrome UX Report API").
# Without a key the extension still works — the CrUX column shows "no data yet".
VITE_CRUX_API_KEY=
